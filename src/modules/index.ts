/**
 * Business modules expose their contracts from `src/modules/<module>`.
 *
 * This root intentionally exports nothing: a global barrel would conceal
 * module dependencies and make accidental coupling easier.
 */
export {};
