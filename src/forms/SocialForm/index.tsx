import { FC } from "react";
import { FormControl } from "@mui/material";

import { SocilalFormProps } from "./Types";

const SocilalForm: FC<SocilalFormProps> = () => {
  return <FormControl>SocilalForm</FormControl>;
};

export default SocilalForm;
