import { FC } from "react";
import { Card } from "@mui/material";

import { ListingDetailsCardProps } from "./Types";

const ListingDetailsContent: FC<ListingDetailsCardProps> = () => {
  return <Card>ListingDetailsContent</Card>;
};

export default ListingDetailsContent;
