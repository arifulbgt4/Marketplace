import { FC } from "react";
import { Box } from "@mui/material";

import { ListingPreviewProps } from "./Types";

const ListingPreview: FC<ListingPreviewProps> = ({ values }) => {
  return <Box>ListingPreview</Box>;
};

export default ListingPreview;
